import { entryType } from '../entries';

export default function getSubstanceAdministration(substanceAdministration) {
  const html = [];

  // html.push(entryType(substanceAdministration));

  return html.join('');
}
