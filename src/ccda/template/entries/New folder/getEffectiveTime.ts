// import { entryType } from '../entries';

export default function getEffectiveTime(effectiveTime){
  const html = [];

//   const { high, low } = effectiveTime;     
  
//   html.push(`
//       <div>
//         <strong>Effective Time:</strong> 
//   `);

//   if (effectiveTime._attributes) {
//     html.push(` `);
//   } else {
//     let time = '';

//     if (high) {
//       time = high._attributes.value;
//     }

//     if (low) {
//       time = low._attributes.value;
//     }

//     html.push(time)
//   }

//   html.push(`
//       </div>
//   `);

  return html.join('');
}