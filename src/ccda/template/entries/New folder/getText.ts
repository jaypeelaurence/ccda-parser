// import { entryType } from '../entries';

function renderText(text) {
  const html = [];

//   html.push(entryType(text));

//   return html.join('');
// }

// export default function getText(text) {
//   const html = [];

//   html.push(renderText(text));

  return html.join('');
}
